{
  "name": "startup-atlas",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "ingest": "pnpm --filter services-pipeline run start"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
